---
# Fluent Bit Service (Optional)
# Exposes Fluent Bit metrics for Prometheus scraping

apiVersion: v1
kind: Service
metadata:
  name: fluent-bit
  namespace: kube-system
  labels:
    app: fluent-bit
    component: logging
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "2020"
    prometheus.io/path: "/api/v1/metrics/prometheus"
spec:
  type: ClusterIP
  clusterIP: None  # Headless service
  selector:
    app: fluent-bit
  ports:
    - name: metrics
      port: 2020
      targetPort: 2020
      protocol: TCP
